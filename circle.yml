
machine:
  node:
    version: 6
  post:
    - npm install -g npm@^5

test:
  override:
    - make ci

deployment:
  release:
    tag: /v.*/
    owner: Financial-Times
    commands:
      - make npm-publish